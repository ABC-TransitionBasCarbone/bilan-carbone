{
  "name": "@abc-transitionbascarbone/publicodes-count",
  "description": "Règles de calculs de count!, le premier calculateur d'impact écologique dédié aux salles de cinéma.",
  "version": "0.1.0",
  "author": "Emile Rolley <emile@calinou.coop>",
  "license": "MIT",
  "type": "module",
  "main": "publicodes-build/index.js",
  "types": "publicodes-build/index.d.ts",
  "files": [
    "publicodes-build"
  ],
  "scripts": {
    "compile": "./node_modules/.bin/publicodes compile",
    "watch": "./node_modules/.bin/publicodes compile --watch",
    "dev": "./node_modules/.bin/publicodes dev",
    "pretest": "yarn run compile",
    "test": "vitest run --reporter=tree"
  },
  "dependencies": {
    "publicodes": "^1.10.1"
  },
  "devDependencies": {
    "@publicodes/tools": "^1.8.1",
    "vitest": "^4.0.9"
  },
  "resolutions": {
    "react-router": ">=7.12.0",
    "minimatch": ">=10.2.1"
  }
}

export const storageKeys = {
  studyFilterSites: (studyId: string) => `study-filter-sites-${studyId}`,
  studyFilterSubposts: (studyId: string) => `study-filter-subposts-${studyId}`,
  studyFilterTags: (studyId: string) => `study-filter-tags-${studyId}`,
}

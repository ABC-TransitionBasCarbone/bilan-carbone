{
  "jobs": [
    {
      "command": "*/10 * * * * yarn user:import",
      "description": "Import users from FTP server at midnight"
    },
    {
      "command": "0 1 * * * npx tsx src/scripts/formationStudies/assignTainingStudy.ts",
      "description": "Create training studies for users who started a formation"
    },
    {
      "command": "30 1 * * * npx tsx src/scripts/formationStudies/assignTainingStudy.ts",
      "description": "Create training correction studies for users who ended their formation"
    }
  ]
}

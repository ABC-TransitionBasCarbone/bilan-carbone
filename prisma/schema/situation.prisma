model Situation {
  id                  String @id @default(uuid())
  situation           Json
  situationListLayout Json   @map("situation_list_layout")

  studySiteId String    @unique @map("study_site_id")
  studySite   StudySite @relation(fields: [studySiteId], references: [id], onDelete: Cascade)

  publicodesVersion String @map("publicodes_version")
  modelVersion      String @map("model_version")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("situations")
}
